include("./llm.ty")
prompt := fs.readText("prompt")
runSentence := (sent) => {
    llmInstance := llm()
    llmInstance.addMessage("user", "{prompt}\n{sent}")
    res := llmInstance.response(0)
    println(res)
}
runSentence(fs.readText(argv[1]))